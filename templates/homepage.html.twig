{# Opnemen van de "Main" file #}
{% extends "main.html.twig" %}

{% block header %}
<img src="assets/img/logo-v2.png"/>
{% endblock %}


{# nu de block content op in de main pagina vullen #}
{% block content %}
    {# Dit is commentaar #}

    {# Print #}
    <h1>Heading #1</h1>
    <h2>Heading #2</h2>
    <h3>Heading #3 t/m 5</h3>

    <h4>Dit is de template {{  title }}</h4>

{# Loop #}
    <div class="grid">
    {% for record in data %}
        <div class= "grid-item">
            <img src="{{ record.afbeelding }}" style="width: 100%; height: 200px;">

            <h2 class ="title"> {{record.titel}} </h2>
                <div class = "container" style = "font-size: 15px"> {{record.korte_omschrijving}} </div>
                    <div class = "container">
                       <div class = "button-container" style = ""> Smullen! </div>
                       <div class="container">
                       <div class = "text-container person">4</div>
                       <div class = "text-container euro">   {{record.prijs}},- </div>
                       <div class = "text-container calories"> {{record.calorieën}}</div>
                       </div>
                    </div>
        </div>
    {% endfor %}
    </div>
     {# Dump #}
       <pre>
        {# {{ dump(data) }} #}
       </pre>

{% endblock %}
